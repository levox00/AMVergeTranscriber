Add more frontend stuff:
- Implement hamburger menu as well as the file explorer
    - Settings will be at top of hamburger menu
      - Settings will have the directory for episode caches, color changes, background changes, more as needed
    - File explorer will be below the hamburger menu
      - File explorer will be styled in a way that it will turn the 2-divided MainLayout to a 3-divided MainLayout,
        user can toggle it on and off to make more space for clip selection
      - Episodes will automatically fill the cache of the file explorer, a name entry will be at the top to let the user know
        that they can type the name of the file/directory
      - One button at top of file explorer to add directory to drag and drop files/directories into
      - Can right click the file explorer, if right clicking a directory, can delete. Can select multiple directories/episodes to right click > delete.
      - Can drag and drop eps into directories in bulk, can rename directories and eps using f2 or right clicking > rename
      
    -  
- Create settings which includes the directly for clip navigation, color changers, background changers, and more

- Bundle everything together into one .exe using npm run tauri build, and use PyInstaller to bundle venv and all
python dependencies, then import that same python into main.rs